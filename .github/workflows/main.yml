name: Add GitHub Actions workflow for manual testing

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: 23f2005699@ds.study.iitm.ac.in
        run: echo "Hello, world!"
